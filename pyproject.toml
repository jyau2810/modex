[project]
name = "modex"
version = "0.1.0"
description = "Configurable multi-account manager and quota watcher for Codex."
requires-python = ">=3.9"
dependencies = []

[project.scripts]
cx = "codex_multi_account.cli:main"
